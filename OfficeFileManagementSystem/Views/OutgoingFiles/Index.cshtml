@model IEnumerable<OfficeFileManagementSystem.Models.OutgoingFile>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Emp_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Incoming_File_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date_Allowed)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Emp_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Incoming_File_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date_Allowed)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Out_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Out_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Out_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
